#!/bin/sh
# ~/.local/bin/kmesg-watcher

journalctl -k -f -n 0 -o cat --output-fields=MESSAGE -p 3 | while read line; do
    [ -z "$line" ] && continue
    notify-send -u critical -t 10000 "Linux Kernel" "$line"
done
