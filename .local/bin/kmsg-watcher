#!/bin/sh
# ~/.local/bin/kmsg-watcher

journalctl -k -f -n 0 -o cat --output-fields=MESSAGE -p 2 | while read line; do
    [ -z "$line" ] && continue
    notify-send -u critical -t 10 "Linux Kernel" "$line"
done
