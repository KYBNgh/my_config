polkit.addRule(function(action, subject) {
    if (!subject.isInGroup("wheel")) {
        return polkit.Result.NO;
    }
    return undefined;
});

polkit.addAdminRule(function(action, subject) {
    if (!subject.isInGroup("wheel")) {
        return ["unix-user:0_never_match"];
    }
    return undefined;
});
