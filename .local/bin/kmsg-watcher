#!/bin/sh
# ~/.local/bin/kmsg-watcher

journalctl -k -f -n 0 -o cat --output-fields=MESSAGE -p 3 | grep -v nouveau | while read line; do
    [ -z "$line" ] && continue
    notify-send -u critical -t 86400000 "Linux Kernel" "$line"
done
