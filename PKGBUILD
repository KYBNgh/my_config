# Maintainer: Arch Linux Team
# Contributor: Levente Polyak <anthraxx[at]archlinux[dot]org>

pkgname=base-devel-doas
pkgver=1
pkgrel=2
pkgdesc='Basic tools to build Arch Linux packages, replacing sudo with doas'
url='https://www.archlinux.org'
arch=('any')
license=('GPL')
options=('!debug')
depends=(
  archlinux-keyring
  autoconf
  automake
  binutils
  bison
  debugedit
  fakeroot
  file
  findutils
  flex
  gawk
  gcc
  gettext
  grep
  groff
  gzip
  libtool
  m4
  make
  pacman
  patch
  pkgconf
  sed
  doas
  texinfo
  which
)

# vim: ts=2 sw=2 et:
