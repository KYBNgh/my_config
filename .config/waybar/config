{
    "layer": "top",
    "position": "top",
    "height": 32,
    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["sway/window"],
    "modules-right": [
        "custom/network-mib",
        "cpu",
        "memory",
        "network",
        "wireplumber",
        "custom/clock",
        "tray",
        "custom/cleaner"
    ],

    "sway/workspaces": {
        "format": "{icon}",
        "on-scroll-up": "swaymsg workspace next",
        "on-scroll-down": "swaymsg workspace prev"
    },

    "sway/mode": {
        "format": "MODE: {}",
        "max-length": 20
    },

   "sway/window": {
    "ellipsis": true
},

 "cpu": {
        "format": " {usage}% ",
        "interval": 1,
        "max-length": 10,
        "on-click": "${HOME}/.local/bin/show_uptime"
    },

    "memory": {
        "format": " {used:0.1f}/{total:0.1f}GiB",
        "interval": 2,
        "max-length": 15
    },

    "network": {
        "format": " {bandwidthDownBytes}/s ↓↑ {bandwidthUpBytes}/s",
        "format-wifi": " {essid}",
        "format-ethernet": "󰈀 {ipaddr}",
        "format-disconnected": "󰅛 Disconnected",
        "tooltip-format": "{ifname}: {ipaddr} / {netmask}",
        "interval": 1,
        "max-length": 20,
        "on-click": "nm-connection-editor"
    },

    "custom/network-mib": {
    "exec": "${XDG_CONFIG_HOME}/waybar/scripts/waybar-net-mib.sh",
    "interval": 1,
    "max-length": 18,
    "return-type": "json",
    "tooltip-format": "Network downlink speed (MiB/s)"
},

    "wireplumber": {
        "format": "{icon} {volume}%",
        "format-muted": "󰝟 {volume}%",
        "format-icons": {
        "headphone": "",
        "default": ["", "", ""]
        },
        "scroll-step": 5
    },

     "custom/clock": {
        "exec": "${XDG_CONFIG_HOME}/waybar/scripts/clock.sh",
        "return-type": "json",
        "on-click": "${HOME}/.local/bin/show_cal"
    },

    "custom/cleaner": {
        "format": "󰎟",
        "interval": "once",
        "on-click": "makoctl dismiss --all",
        "tooltip-format": "Clean all notifications"
    }
}

